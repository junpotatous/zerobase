let age = 20;

if (age < 19) {
  msg = "The user is not an audlt";
} else {
  msg = "The user is an audlt"
}

console.log(msg);

msg_another = (age < 19) ? "The user is not an audlt" : "The user is an audlt";
console.log(msg_another;)